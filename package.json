{
  "name": "first-crud-api",
  "version": "1.0.0",
  "description": "A small CRUD API for managing a to-do list, documented and testable via Swagger UI.",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^5.2.1",
    "swagger-ui-express": "^5.0.1"
  }
}
